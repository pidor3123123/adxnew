# 🔍 Диагностика проблем регистрации на Hostinger

Это руководство поможет вам найти и исправить проблемы с регистрацией на сайте ADX Finance.

## 📋 Быстрая диагностика

### Шаг 1: Использование диагностического скрипта

1. **Загрузите обновленный архив**
   - Убедитесь, что вы загрузили последний `adx-finance-deploy.zip`
   - Распакуйте его в корневую директорию сайта (обычно `public_html` или `domains/adx.finance/public_html`)

2. **Откройте диагностический скрипт**
   - В браузере перейдите по адресу: `https://adx.finance/check_registration.php`
   - Скрипт автоматически проверит все необходимые условия

3. **Проверьте результаты**
   - ✅ Зеленые сообщения = все хорошо
   - ❌ Красные сообщения = есть проблемы, которые нужно исправить
   - ⚠️ Желтые сообщения = предупреждения, но не критично

### Что проверяет скрипт:

- ✅ Загрузка конфигурации базы данных
- ✅ Подключение к базе данных
- ✅ Существование таблицы `users`
- ✅ Существование таблицы `user_sessions`
- ✅ Существование таблицы `balances`
- ✅ Структура таблицы `users` (наличие всех колонок)
- ✅ Доступность файла `api/auth.php`

---

## 🗄️ Шаг 2: Создание таблиц базы данных

### Если скрипт показал, что таблицы не существуют:

1. **Откройте setup.php**
   - В браузере перейдите: `https://adx.finance/setup.php`
   - Скрипт автоматически создаст все необходимые таблицы

2. **Проверьте результат**
   - Вы увидите сообщение об успешном создании таблиц
   - Если есть ошибки, они будут показаны на экране

3. **Повторная проверка**
   - Откройте снова `check_registration.php`
   - Теперь все проверки таблиц должны пройти успешно

### Альтернативный способ (через phpMyAdmin):

1. Войдите в hPanel Hostinger
2. Найдите раздел "База данных" → "phpMyAdmin"
3. Выберите вашу базу данных
4. Перейдите на вкладку "Импорт" (Import)
5. Загрузите файл `database.sql` из архива
6. Нажмите "Выполнить" (Go)

---

## ⚙️ Шаг 3: Проверка настроек базы данных

### Если скрипт показал ошибку подключения к БД:

#### Способ 1: Через панель управления Hostinger

1. **Войдите в hPanel**
   - Откройте https://hpanel.hostinger.com
   - Войдите в свой аккаунт

2. **Найдите данные базы данных**
   - Перейдите в раздел "База данных" → "MySQL Databases"
   - Или "База данных" → "Управление базами данных"
   - Найдите вашу базу данных (обычно начинается с `u` и цифр)

3. **Скопируйте данные:**
   - **Имя базы данных** (Database Name) - например: `u123456789_novatrade`
   - **Имя пользователя** (Username) - например: `u123456789_user`
   - **Пароль** (Password) - ваш пароль от БД
   - **Хост** (Host) - обычно `localhost` или IP адрес

#### Способ 2: Через File Manager

1. **Откройте File Manager в hPanel**
   - Найдите раздел "Файлы" → "File Manager"
   - Перейдите в корневую директорию сайта

2. **Откройте файл конфигурации**
   - Найдите файл `config/database.php`
   - Нажмите правой кнопкой → "Редактировать" (Edit)

3. **Проверьте и обновите настройки:**

```php
<?php
define('DB_HOST', 'localhost'); // или IP адрес сервера БД из hPanel
define('DB_NAME', 'u123456789_novatrade'); // имя БД из hPanel
define('DB_USER', 'u123456789_user'); // имя пользователя из hPanel
define('DB_PASS', 'ваш_пароль_от_БД'); // пароль из hPanel
define('DB_CHARSET', 'utf8mb4');
```

4. **Сохраните файл**
   - Нажмите "Сохранить" (Save)
   - Закройте редактор

5. **Проверьте снова**
   - Откройте `check_registration.php`
   - Проверка подключения должна пройти успешно

---

## 📝 Шаг 4: Проверка логов ошибок PHP

### Где найти логи на Hostinger:

#### Способ 1: Через hPanel

1. **Войдите в hPanel**
2. **Найдите раздел "Логи"**
   - Обычно находится в разделе "Дополнительно" или "Мониторинг"
   - Или "Файлы" → "Логи ошибок"

3. **Откройте PHP Error Log**
   - Найдите файл с названием типа `error_log` или `php_error.log`
   - Или "Просмотр логов" → "PHP Error Log"

4. **Найдите ошибки**
   - Найдите записи с временем, когда вы пытались зарегистрироваться
   - Обратите внимание на строки с `api/auth.php`
   - Скопируйте сообщения об ошибках

#### Способ 2: Через File Manager

1. **Откройте File Manager**
2. **Найдите файлы логов:**
   - В корневой директории: `error_log`
   - В папке `api/`: `.error_log` или `error_log`
   - В папке `logs/`: различные файлы логов

3. **Откройте файл**
   - Нажмите правой кнопкой → "Редактировать" или "Просмотр"
   - Найдите последние записи (они внизу файла)

4. **Типичные ошибки и их решения:**

```
Ошибка: Table 'database.users' doesn't exist
Решение: Запустите setup.php для создания таблиц

Ошибка: Access denied for user 'username'@'localhost'
Решение: Проверьте имя пользователя и пароль в config/database.php

Ошибка: Unknown database 'database_name'
Решение: Проверьте имя базы данных в config/database.php

Ошибка: Call to undefined function getDB()
Решение: Проверьте, что config/database.php загружается правильно
```

---

## 🔐 Шаг 5: Проверка прав доступа к файлам

### Если есть проблемы с доступом к файлам:

1. **Откройте File Manager в hPanel**

2. **Найдите папку `api/`**
   - Перейдите в корневую директорию сайта
   - Найдите папку `api`

3. **Проверьте права доступа:**
   - Нажмите правой кнопкой на папку `api/` → "Права доступа" (Permissions)
   - Должны быть установлены: `755` или `775`
   - Если нет, установите `755`

4. **Проверьте файл `auth.php`:**
   - Нажмите правой кнопкой на `api/auth.php` → "Права доступа"
   - Должны быть установлены: `644` или `755`
   - Если нет, установите `644`

5. **Проверьте папку `config/`:**
   - Права доступа: `755`
   - Файл `database.php`: `644`

### Как установить права доступа:

1. Выберите файл/папку
2. Правой кнопкой → "Права доступа" (Permissions) или "Change Permissions"
3. Введите нужные права (например, `644` или `755`)
4. Отметьте "Рекурсивно" (Recursive) для папок
5. Нажмите "Изменить" (Change)

---

## 🚨 Частые проблемы и решения

### Проблема 1: Пустой ответ от сервера (Content-Length: 0)

**Причины:**
- Таблицы базы данных не существуют
- Фатальная ошибка PHP до вывода JSON
- Проблема с правами доступа к файлам

**Решение:**
1. Запустите `check_registration.php` для диагностики
2. Если таблицы не существуют → запустите `setup.php`
3. Проверьте логи ошибок PHP
4. Проверьте права доступа к файлам

### Проблема 2: Ошибка 500 Internal Server Error

**Причины:**
- Ошибка в коде PHP
- Проблема с базой данных
- Неправильные настройки

**Решение:**
1. Откройте `check_registration.php` для диагностики
2. Проверьте логи ошибок PHP
3. Убедитесь, что все таблицы созданы
4. Проверьте настройки базы данных

### Проблема 3: "Failed to execute 'json' on 'Response'"

**Причины:**
- Сервер возвращает не JSON ответ
- Пустой ответ от сервера
- Ошибка в обработке ответа

**Решение:**
1. Проверьте логи ошибок PHP
2. Убедитесь, что `api/auth.php` возвращает валидный JSON
3. Проверьте, что все таблицы созданы
4. Проверьте права доступа к файлам

### Проблема 4: "Ошибка подключения к базе данных"

**Причины:**
- Неправильные данные в `config/database.php`
- База данных не создана
- Неправильный хост

**Решение:**
1. Проверьте данные БД в hPanel
2. Обновите `config/database.php` с правильными данными
3. Убедитесь, что база данных создана в hPanel

---

## 📞 Дополнительная помощь

### Если ничего не помогло:

1. **Проверьте версию PHP:**
   - В hPanel найдите "PHP Configuration"
   - Убедитесь, что используется PHP 7.4 или выше (рекомендуется 8.0+)

2. **Проверьте расширения PHP:**
   - Должны быть включены: `pdo`, `pdo_mysql`, `curl`, `json`

3. **Свяжитесь с поддержкой Hostinger:**
   - Если проблема не решается, обратитесь в поддержку
   - Приложите скриншоты из `check_registration.php`
   - Приложите логи ошибок PHP

---

## ✅ Чек-лист перед использованием

Перед тем как использовать регистрацию, убедитесь:

- [ ] Все файлы загружены на сервер
- [ ] База данных создана в hPanel
- [ ] Настройки в `config/database.php` правильные
- [ ] Таблицы созданы (через `setup.php` или `database.sql`)
- [ ] `check_registration.php` показывает все зеленые галочки
- [ ] Права доступа к файлам установлены правильно
- [ ] PHP версия 7.4 или выше

---

## 🔒 Безопасность

**ВАЖНО:** После проверки и исправления всех проблем:

1. **Удалите диагностический скрипт:**
   - Удалите файл `check_registration.php` с сервера
   - Он содержит информацию о структуре базы данных

2. **Удалите setup.php (если не нужен):**
   - После создания таблиц можно удалить `setup.php`
   - Или ограничьте доступ к нему через `.htaccess`

3. **Защитите config/database.php:**
   - Убедитесь, что файл не доступен напрямую через браузер
   - Обычно это работает автоматически, но проверьте

---

## 📚 Полезные ссылки

- Документация Hostinger: https://support.hostinger.com
- Документация PHP: https://www.php.net/manual/ru/
- Документация MySQL: https://dev.mysql.com/doc/

---

**Последнее обновление:** 25 января 2026
