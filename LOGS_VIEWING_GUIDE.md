# Руководство по просмотру логов в реальном времени

## Где находятся логи?

Логи хранятся в **localStorage браузера** под ключом `adx_finance_logs`. Это означает, что логи доступны только в том браузере, где они были созданы.

## Способы просмотра логов

### 1. Через консоль браузера (быстрый способ)

1. Откройте сайт (например, `https://adx.finance`)
2. Нажмите **F12** (или **Ctrl+Shift+I**) для открытия инструментов разработчика
3. Перейдите на вкладку **Console**
4. Выполните команды:

```javascript
// Просмотр всех логов
Logger.getLogs()

// Просмотр только ошибок
Logger.getLogs().filter(log => log.includes('[ERROR]') || log.includes('[API_ERROR]'))

// Просмотр последних 50 логов
Logger.getLogs().slice(-50)

// Экспорт всех логов в текстовый формат
console.log(Logger.exportLogs())

// Очистка логов
Logger.clearLogs()
```

### 2. Через Application/Storage tab (визуальный способ)

1. Откройте инструменты разработчика (F12)
2. Перейдите на вкладку **Application** (Chrome) или **Storage** (Firefox)
3. В левой панели найдите **Local Storage**
4. Выберите ваш домен (`https://adx.finance`)
5. Найдите ключ `adx_finance_logs`
6. Кликните на значение - откроется JSON с массивом логов

### 3. Через специальную страницу просмотра (рекомендуется)

1. Откройте файл `view_logs.html` в браузере
   - Локально: откройте файл напрямую
   - На сервере: откройте `https://adx.finance/view_logs.html`
2. Страница автоматически загрузит все логи из localStorage
3. Используйте фильтры для поиска нужных логов
4. Включите автообновление для просмотра в реальном времени

**Возможности страницы просмотра:**
- ✅ Фильтрация по типу логов (ошибки, API запросы, действия пользователей и т.д.)
- ✅ Поиск по содержимому логов
- ✅ Статистика (общее количество, ошибки, API запросы)
- ✅ Автообновление каждые 5 секунд
- ✅ Экспорт и скачивание логов
- ✅ Очистка логов

### 4. Через Network tab (для API запросов)

1. Откройте инструменты разработчика (F12)
2. Перейдите на вкладку **Network**
3. Все API запросы будут видны в реальном времени
4. Кликните на любой запрос для просмотра деталей:
   - **Headers** - заголовки запроса/ответа
   - **Payload** - данные запроса
   - **Response** - ответ сервера
   - **Timing** - время выполнения

## Типы логов

Система логирования записывает следующие типы событий:

- **`ERROR`** - JavaScript ошибки
- **`API_ERROR`** - Ошибки при API запросах
- **`API_REQUEST`** - Все API запросы (URL, метод, параметры, время ответа)
- **`USER_ACTION`** - Действия пользователей (клики, отправка форм)
- **`TRADE`** - Торговые операции
- **`STATE_CHANGE`** - Изменения состояния (баланс, авторизация, выбор актива)
- **`NAVIGATION`** - Переходы между страницами
- **`INFO`** - Информационные сообщения
- **`WARN`** - Предупреждения
- **`SYSTEM`** - Системные события (инициализация, загрузка страницы)

## Просмотр логов мониторинга БД

Логи мониторинга соединений с базами данных также сохраняются в общий лог:

```javascript
// Фильтрация логов мониторинга БД
Logger.getLogs().filter(log => 
    log.includes('Database') || 
    log.includes('DBMonitor') || 
    log.includes('MySQL') || 
    log.includes('Supabase')
)
```

Или через страницу просмотра:
- Выберите фильтр "INFO" и введите "Database" в поиск

## Экспорт логов

### Способ 1: Через консоль

```javascript
// Получить все логи как текст
const logsText = Logger.exportLogs();

// Скопировать в буфер обмена
navigator.clipboard.writeText(logsText);

// Или вывести в консоль
console.log(logsText);
```

### Способ 2: Через страницу просмотра

1. Откройте `view_logs.html`
2. Нажмите кнопку **"Экспорт"** - логи появятся в текстовом поле
3. Нажмите **"Скачать"** - файл будет загружен на компьютер

### Способ 3: Прямое скачивание из localStorage

```javascript
// Создать и скачать файл
const logs = Logger.getLogs();
const text = logs.join('\n');
const blob = new Blob([text], { type: 'text/plain' });
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = `logs-${new Date().toISOString().split('T')[0]}.txt`;
a.click();
```

## Очистка логов

Логи автоматически ограничиваются до 1000 записей (самые старые удаляются).

Для ручной очистки:

```javascript
// Через консоль
Logger.clearLogs()

// Или напрямую
localStorage.removeItem('adx_finance_logs')
```

## Просмотр логов на сервере (PHP)

Если нужно проверить серверные логи:

1. **Через панель Hostinger:**
   - Войдите в hPanel
   - Перейдите в **"Файлы"** → **"File Manager"**
   - Найдите файлы логов (обычно в `/logs/` или `/var/log/`)

2. **Через SSH (если доступен):**
   ```bash
   # PHP ошибки
   tail -f /var/log/php_errors.log
   
   # Apache ошибки
   tail -f /var/log/apache2/error.log
   
   # Или через панель Hostinger → Advanced → SSH Access
   ```

3. **Через панель Hostinger → Advanced → Error Log:**
   - Просмотр последних ошибок PHP

## Полезные команды для отладки

```javascript
// Просмотр последних 10 ошибок
Logger.getLogs()
    .filter(log => log.includes('[ERROR]') || log.includes('[API_ERROR]'))
    .slice(-10)

// Просмотр всех API запросов за последний час
Logger.getLogs()
    .filter(log => log.includes('[API_REQUEST]'))
    .filter(log => {
        const timestamp = log.match(/\[([^\]]+)\]/);
        if (timestamp) {
            const logTime = new Date(timestamp[1]);
            const hourAgo = new Date(Date.now() - 3600000);
            return logTime > hourAgo;
        }
        return false;
    })

// Поиск логов по конкретному URL
Logger.getLogs()
    .filter(log => log.includes('/api/wallet.php'))

// Просмотр всех действий конкретного пользователя (если логируется user_id)
Logger.getLogs()
    .filter(log => log.includes('user_id') && log.includes('ваш_user_id'))
```

## Ограничения

- **Логи хранятся только в браузере** - при очистке localStorage или смене браузера логи теряются
- **Максимум 1000 записей** - старые логи автоматически удаляются
- **Логи не синхронизируются** между устройствами/браузерами
- **Логи доступны только на клиенте** - сервер не имеет доступа к этим логам

## Рекомендации

1. **Регулярно экспортируйте логи** для архивирования важной информации
2. **Используйте страницу просмотра** (`view_logs.html`) для удобного анализа
3. **Включайте автообновление** при отладке проблем в реальном времени
4. **Фильтруйте по типу** для быстрого поиска нужных событий
5. **Очищайте логи периодически** для освобождения места в localStorage

## Устранение проблем

### Проблема: Логи не отображаются

**Решение:**
1. Проверьте, что `js/logger.js` загружен (Network tab)
2. Проверьте консоль на наличие ошибок JavaScript
3. Проверьте, что localStorage доступен (не в режиме инкогнито)

### Проблема: Логи переполняют localStorage

**Решение:**
1. Логи автоматически ограничиваются до 1000 записей
2. Если все еще проблема - очистите логи вручную: `Logger.clearLogs()`
3. Или экспортируйте и удалите старые логи

### Проблема: Страница просмотра не работает

**Решение:**
1. Убедитесь, что вы открываете страницу на том же домене, где работал сайт
2. Проверьте, что localStorage содержит ключ `adx_finance_logs`
3. Откройте консоль и проверьте наличие ошибок

---

**Готово!** Теперь вы знаете, как просматривать логи в реальном времени. 🎉
