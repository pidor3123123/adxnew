# Инструкция по деплою на Hostinger

## 📋 Обзор

Этот проект состоит из двух частей:
1. **PHP API + Frontend** - загружается на Hostinger
2. **Next.js Админ панель** - деплоится на Vercel (отдельно)

Данная инструкция описывает деплой PHP части на Hostinger.

## 🚀 Шаг 1: Подготовка файлов

### Что нужно загрузить на Hostinger:

1. Откройте папку `hostinger-deploy/` в проекте
2. Заархивируйте **ВСЕ** файлы из этой папки в ZIP архив
3. Имя архива: `adxfinance-hostinger.zip` (или любое другое)

### Структура файлов для загрузки:

```
hostinger-deploy/
├── api/              # PHP API файлы
├── config/           # Конфигурационные файлы
├── css/              # Стили
├── js/               # JavaScript файлы
├── assets/           # Изображения и ресурсы (если есть)
├── *.html            # HTML страницы (index.html, login.html и т.д.)
├── database.sql      # Структура базы данных
├── .htaccess         # Конфигурация Apache
├── check_db.php      # Скрипт проверки (временно)
├── sync_to_supabase.php
├── setup.php
└── README.txt
```

## 📤 Шаг 2: Загрузка файлов на Hostinger

### Вариант A: Через File Manager (рекомендуется)

1. Войдите в панель Hostinger (hPanel)
2. Перейдите в **"Файлы"** → **"File Manager"**
3. Откройте папку `public_html` (или `domains/ваш-домен.com/public_html`)
4. Удалите все файлы по умолчанию (index.html, если есть)
5. Нажмите **"Загрузить"** (Upload)
6. Выберите ваш ZIP архив
7. Дождитесь завершения загрузки
8. Выделите загруженный ZIP файл
9. Нажмите **"Распаковать"** (Extract)
10. Удалите ZIP файл после распаковки

### Вариант B: Через FTP

1. Используйте FTP клиент (FileZilla, WinSCP и т.д.)
2. Подключитесь к серверу Hostinger:
   - **Хост**: `ftp.ваш-домен.com` или IP адрес
   - **Пользователь**: ваш FTP пользователь
   - **Пароль**: ваш FTP пароль
   - **Порт**: 21
3. Перейдите в папку `public_html`
4. Загрузите все файлы из папки `hostinger-deploy/`
5. Убедитесь, что структура папок сохранена

## 🗄️ Шаг 3: Настройка базы данных

Следуйте инструкции в файле `DATABASE_SETUP.md` в папке `hostinger-deploy/`

**Кратко:**
1. Создайте базу данных MySQL в панели Hostinger
2. Импортируйте `database.sql` через phpMyAdmin
3. Запишите данные подключения (хост, имя БД, пользователь, пароль)

## ⚙️ Шаг 4: Настройка переменных окружения

1. В File Manager создайте файл `.env` в корневой папке (`public_html`)
2. Скопируйте следующий шаблон и заполните реальными данными:

```env
# База данных MySQL
DB_HOST=localhost
DB_NAME=ваше_имя_базы_данных
DB_USER=ваш_пользователь_базы_данных
DB_PASS=ваш_пароль_базы_данных
DB_CHARSET=utf8mb4

# Supabase (для синхронизации с админ панелью)
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш_ключ_supabase

# Webhook (URL вашего сайта)
WEBHOOK_URL=https://ваш-домен.com/api/webhook.php
WEBHOOK_SECRET=сгенерируйте_сложный_секретный_ключ

# CORS (разрешенные домены)
ALLOWED_ORIGINS=https://ваш-домен.com,https://админ-панель.vercel.app
```

**Важно:**
- Замените все значения на реальные
- Для `WEBHOOK_SECRET` сгенерируйте случайную строку (минимум 32 символа)
- Для `ALLOWED_ORIGINS` укажите домены вашего сайта и админ панели

### Как сгенерировать секретный ключ:

```bash
# В терминале Linux/Mac:
openssl rand -base64 32

# Или используйте онлайн генератор:
# https://www.random.org/strings/
```

## ✅ Шаг 5: Проверка установки

1. Загрузите файл `check_db.php` на сервер (если еще не загружен)
2. Откройте в браузере: `https://ваш-домен.com/check_db.php`
3. Проверьте результаты:
   - ✅ Все проверки должны быть зелеными
   - ❌ Если есть ошибки - исправьте их

4. **ВАЖНО**: После успешной проверки **удалите** `check_db.php` с сервера!

## 🔒 Шаг 6: Настройка прав доступа

Убедитесь, что права доступа установлены правильно:

- **Файлы**: 644 (rw-r--r--)
- **Папки**: 755 (rwxr-xr-x)
- **.env файл**: 600 (rw-------) - только владелец может читать/писать

В File Manager:
1. Выделите файл/папку
2. Правый клик → **"Изменить права"** (Change Permissions)
3. Установите нужные права

## 🌐 Шаг 7: Проверка работы сайта

1. Откройте в браузере: `https://ваш-домен.com`
2. Должна открыться главная страница
3. Попробуйте:
   - Зарегистрироваться: `/register.html`
   - Войти: `/login.html`
   - Проверить API: `/api/market.php?action=crypto`

## 🔧 Шаг 8: Настройка SSL (HTTPS)

Hostinger обычно предоставляет бесплатный SSL сертификат:

1. В панели Hostinger перейдите в **"SSL"**
2. Активируйте бесплатный SSL сертификат (Let's Encrypt)
3. Дождитесь активации (обычно несколько минут)
4. Убедитесь, что сайт открывается по HTTPS

## 📝 Шаг 9: Настройка домена (если нужно)

Если используете поддомен или другой домен:

1. В панели Hostinger перейдите в **"Домены"**
2. Добавьте домен или поддомен
3. Настройте DNS записи (если нужно)
4. Обновите `ALLOWED_ORIGINS` в `.env` файле

## 🚨 Решение проблем

### Проблема: "Неподдерживаемый фреймворк"

**Решение**: Hostinger не распознает смешанную структуру (Next.js + PHP). 
- Загружайте только PHP файлы из папки `hostinger-deploy/`
- Next.js админ панель деплойте отдельно на Vercel

### Проблема: "500 Internal Server Error"

**Решение**:
1. Проверьте права доступа к файлам (644 для файлов, 755 для папок)
2. Проверьте `.htaccess` файл (должен быть в корне)
3. Проверьте логи ошибок в панели Hostinger
4. Убедитесь, что PHP версия 7.4 или выше

### Проблема: "Database connection failed"

**Решение**:
1. Проверьте данные в `.env` файле
2. Убедитесь, что база данных создана и импортирована
3. Проверьте, что пользователь БД имеет права доступа
4. Используйте `check_db.php` для диагностики

### Проблема: "CORS error" в браузере

**Решение**:
1. Проверьте `ALLOWED_ORIGINS` в `.env`
2. Убедитесь, что домены указаны правильно (с протоколом https://)
3. Перезагрузите страницу после изменения `.env`

### Проблема: API не отвечает

**Решение**:
1. Проверьте, что файлы в папке `api/` загружены
2. Проверьте `.htaccess` - должен разрешать доступ к API
3. Проверьте логи ошибок PHP
4. Попробуйте открыть API напрямую: `/api/market.php?action=crypto`

## 📚 Дополнительные ресурсы

- [Документация Hostinger](https://www.hostinger.com/tutorials)
- [Настройка PHP на Hostinger](https://www.hostinger.com/tutorials/how-to-change-php-version)
- [Настройка базы данных](hostinger-deploy/DATABASE_SETUP.md)

## ✅ Чек-лист после деплоя

- [ ] Все файлы загружены на сервер
- [ ] База данных создана и импортирована
- [ ] Файл `.env` создан и заполнен
- [ ] Права доступа установлены правильно
- [ ] `check_db.php` показывает все проверки успешными
- [ ] `check_db.php` удален с сервера
- [ ] Сайт открывается по HTTPS
- [ ] Регистрация и вход работают
- [ ] API endpoints отвечают
- [ ] SSL сертификат активирован

## 🎉 Готово!

После выполнения всех шагов ваш PHP API должен работать на Hostinger.

**Следующий шаг**: Задеплойте Next.js админ панель на Vercel (см. `VERCEL_DEPLOY.md`)
